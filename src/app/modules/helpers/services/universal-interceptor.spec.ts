import { UniversalInterceptor } from './universal-interceptor';

describe('UniversalInterceptor', () => {
  it('should create an instance', () => {
    expect(new UniversalInterceptor()).toBeTruthy();
  });
});
